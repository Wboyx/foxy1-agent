# طرح Foxy1 Agent — اتوماسیون گفت‌وگومحور

نسخه طرح: 2026-08-16
وضعیت: طراحی، هنوز ساخته نشده

---

## ۱. چالش اصلی و راه‌حل آن

انتخاب تو این بود که مغز روی سرویس رایگان ابری باشد. یک مانع فنی دارد:

سرویس‌های رایگان ابری بدون سرور اجرا می‌شوند و نمی‌توانند اتصال SSH پایدار نگه دارند. همچنین نمی‌توانند به سرور ایران وصل شوند، چون سرور ایران پورت ورودی باز ندارد و نباید داشته باشد.

راه‌حل: جهت اتصال را برعکس می‌کنیم.

به‌جای اینکه مغز به سرور وصل شود، یک عامل کوچک روی سرور می‌نشیند و خودش هر چند ثانیه از مغز می‌پرسد کاری هست یا نه.

```text
Pull به‌جای Push
```

مزیت‌های این معماری:

- هیچ پورت ورودی روی سرور ایران باز نمی‌شود
- کلید SSH هیچ‌جا ذخیره نمی‌شود
- فیلترینگ مانع نیست، چون سرور ایران به vercel.app دسترسی دارد
- اگر مغز از کار بیفتد، سرور سالم می‌ماند

---

## ۲. معماری کلی

```text
تو در تلگرام
      ↓
ربات Foxy1 (Webhook)
      ↓
مغز روی Vercel
      ├── حافظه و صف دستور روی پایگاه داده رایگان
      ├── روتر مدل: Gemini، Groq، OpenRouter
      └── قوانین Foxy1 به‌عنوان System Prompt
      ↓
صف دستورهای تأییدشده
      ↑ (سرور می‌پرسد، مغز جواب می‌دهد)
عامل کوچک روی سرور ایران
      ↓
اجرای دستور و بازگرداندن خروجی
```

---

## ۳. اجزا

### الف. مغز روی Vercel

وظیفه:

- دریافت پیام تلگرام
- تصمیم‌گیری با کمک مدل زبانی
- ساخت دستور پیشنهادی
- درخواست تأیید از تو
- ثبت نتیجه در حافظه

نکته: بدون حالت اجرا می‌شود، پس حافظه باید بیرون باشد.

گزینه پایگاه داده رایگان:

```text
Upstash Redis
Neon Postgres
Turso SQLite
```

### ب. روتر مدل

هیچ سرویس رایگانی به‌تنهایی نامحدود نیست. پس چند سرویس را روی هم می‌گذاریم و بین آن‌ها می‌چرخیم.

سقف‌های واقعی در تاریخ طرح:

```text
Gemini Flash    : 1500 درخواست در روز، 15 در دقیقه
Groq            : 1000 تا 14400 درخواست در روز، 30 در دقیقه
OpenRouter      : 50 تا 1000 درخواست در روز، 20 در دقیقه
Cerebras        : حدود یک میلیون توکن در روز
```

جمع تقریبی برای یک نفر عملاً کافی است.

منطق انتخاب مدل بر اساس نوع کار:

```text
خواندن لاگ و خلاصه ساده      -> Groq (سریع و سهمیه بالا)
تحلیل و دیباگ پیچیده          -> Gemini Flash (زمینه یک میلیونی)
نظر دوم در تصمیم پرریسک       -> OpenRouter (مدل متفاوت)
```

قانون مهم: اگر یک ارائه‌دهنده سهمیه‌اش تمام شد، خودکار به بعدی برود و به تو اطلاع دهد.

### ج. عامل روی سرور ایران

یک اسکریپت کوچک، حدود دویست خط، بدون وابستگی سنگین.

چرخه کاری:

۱. هر پنج ثانیه از مغز می‌پرسد دستور تأییدشده‌ای هست یا نه
۲. اگر بود، ابتدا آن را با قوانین ایمنی می‌سنجد
۳. اجرا می‌کند با محدودیت زمان
۴. خروجی را برمی‌گرداند
۵. همه‌چیز را در لاگ محلی ثبت می‌کند

مصرف منابع هدف:

```text
RAM: کمتر از 30 MB
CPU: نزدیک صفر در حالت انتظار
```

---

## ۴. لایه ایمنی — مهم‌ترین بخش

بدون این بخش، اتوماسیون یعنی ریسک نابودی داده.

### چهار سطح دفاع

سطح یک — لیست سیاه در خود عامل:

این دستورها حتی اگر مغز بفرستد و تو هم تأیید کنی، اجرا نمی‌شوند مگر با عبارت امنیتی جداگانه:

```text
rm -rf
mkfs
dd
DROP TABLE
DELETE FROM
netplan apply
reboot
shutdown
iptables -F
chmod 777
> /etc/
```

دلیل تأکید بر netplan: حادثه سرور نیما در تاریخ 2026-08-14 که با یک فایل اشتباه، سرور از دسترس خارج شد.

سطح دو — تأیید دستی تو:

هیچ دستوری بدون فشردن دکمه تأیید در تلگرام اجرا نمی‌شود.

پیام تأیید باید نشان دهد:

```text
دستور دقیق
هدف آن
ریسک تخمینی
دستور بازگشت
```

سطح سه — بکاپ خودکار:

اگر دستور به فایلی دست می‌زند، عامل قبل از اجرا خودش بکاپ Timestampدار می‌گیرد.

سطح چهار — ثبت کامل:

هر دستور، هر خروجی، هر تصمیم مدل، در فایل لاگ محلی روی سرور ثبت می‌شود. مستقل از مغز.

### قانون قرمز

عامل هرگز نباید کاری کند که ترافیک کاربر نهایی را از سرور ایران عبور دهد. این قانون در خود کد عامل به‌صورت لیست سیاه پیاده می‌شود، نه فقط در پرامپت.

### قانون Secret

مغز روی سرویس ابری است و مدل‌های رایگان ممکن است داده را برای آموزش استفاده کنند. بنابراین:

```text
هیچ Token، Password، Private Key یا خروجی حاوی آن‌ها
نباید به سمت مغز فرستاده شود
```

عامل قبل از ارسال خروجی، الگوهای حساس را خودش Redact می‌کند.

---

## ۵. فازبندی

### فاز صفر — پایه و پایش

هدف: بدون هیچ ریسکی، ارزش واقعی بگیریم.

- ربات تلگرام جدید و جدا از ربات فروشگاه
- عامل فقط در حالت خواندنی
- پایش خودکار: وضعیت سرویس‌ها، RAM، Swap، دیسک، گواهی، سلامت پروکسی
- هشدار فعال در تلگرام

خروجی: اگر امشب تکرار شود، خودش قبل از تو می‌فهمد و خبر می‌دهد.

معیار پذیرش: هفت روز کار بدون خطا و بدون مصرف بیش از حد منابع.

### فاز یک — دیباگ گفت‌وگومحور

- اتصال روتر مدل
- توانایی خواندن لاگ و تحلیل
- پیشنهاد دستور، بدون اجرا

معیار پذیرش: بتواند علت یک مشکل واقعی را درست تشخیص دهد.

### فاز دو — اجرای با تأیید

- دکمه تأیید در تلگرام
- لیست سفید دستورها
- بکاپ خودکار
- دستور بازگشت آماده

معیار پذیرش: ده عملیات موفق پشت سر هم بدون از دست رفتن داده.

### فاز سه — مشورت چندمدلی

- برای تصمیم‌های پرریسک، دو مدل مستقل نظر بدهند
- اگر اختلاف داشتند، به تو گزارش شود

---

## ۶. آنچه این سیستم نخواهد بود

طبق قانون صداقت Foxy1:

- جایگزین کامل یک دستیار پیشرفته نیست؛ مدل‌های رایگان ضعیف‌ترند
- بدون نظارت تو کار نمی‌کند و نباید بکند
- تضمین بی‌نقص بودن ندارد
- سرویس‌های رایگان ممکن است سهمیه‌شان را تغییر دهند

---

## ۷. برآورد هزینه

```text
هزینه مالی ماهانه: صفر
هزینه واقعی: زمان راه‌اندازی و نگهداری
```

نقطه شکست احتمالی: اگر روزی سهمیه رایگان تمام شود یا دامنه سرویس فیلتر شود. برای همین روتر مدل و جدول دامنه‌های جایگزین از ابتدا لازم است.

---

## ۸. مرحله پیشنهادی بعدی

ساخت فاز صفر. کوچک، امن، و بلافاصله مفید.

قدم اول عملی: ساخت یک ربات تلگرام جدید و جدا برای Foxy1، تا هیچ ارتباطی با ربات فروشگاه نداشته باشد.
